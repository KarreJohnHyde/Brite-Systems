# Full pinned Streamlit deployment for CPython 3.11+.
# Install with: python -m pip install -r requirements.txt

# Community Cloud installs this aggregate so every sidebar runtime is present.
-r requirements-ui.txt
-r requirements-ml.txt
-r requirements-llm.txt
-r requirements-tracing.txt
